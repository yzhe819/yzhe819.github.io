---
title: 操作系统实战第二讲笔记 | 几行汇编几行C：实现一个最简单的内核
date: 2021/7/3
sidebar: 'false'
prev: ./000001
next: false
categories:
 - 操作系统
tags:
 - 操作系统
 - 实战
---
::: tip
用CRUB让Hello OS在硬件上跑起来
:::
<!-- more -->


## 02 笔记 | 几行汇编几行C：实现一个最简单的内核
[[toc]]

### PC机的引导流程

![f2d31ab7144bf309761711efa9d6d4bd](https://github.com/yzhe819/Cosmos/raw/master/resource/img/f2d31ab7144bf309761711efa9d6d4bd.jpg)

当 x86 PC 启动时，它执行的是一个叫 BIOS 的程序。BIOS 存放在非易失存储器中，BIOS  的作用是在启动时进行硬件的准备工作，接着把控制权交给操作系统。具体来说，BIOS  会把控制权交给从引导扇区（用于引导的磁盘的第一个512字节的数据区）加载的代码。引导扇区中包含引导加载器（CRUB引导程序）——负责内核加载到内存中。BIOS  会把引导扇区加载到内存 0x7c00 处，接着（通过设置寄存器 `%ip`）跳转至该地址。因为知识尚且不足，我们借用 GRUB 引导程序来引导我们的HelloOs。



### 为什么是0x7c00? 内存里面发生了什么？

简单说，计算机启动是这样一个过程。

> 1. 通电
> 2. 读取ROM里面的BIOS，用来检查硬件
> 3. 硬件检查通过
> 4. **BIOS根据指定的顺序，检查引导设备的第一个扇区（即主引导记录），加载在内存地址 0x7C00**
> 5. 主引导记录把操作权交给操作系统



**所以，主引导记录就是引导"操作系统"进入内存的一段小程序，大小不超过1个扇区（512字节）**

0x7C00这个地址来自Intel的第一代个人电脑芯片[8088](https://en.wikipedia.org/wiki/Intel_8088)，以后的CPU为了保持兼容，一直使用这个地址。



1981年8月，IBM公司最早的个人电脑IBM PC 5150上市，就用了这个芯片。

![img](https://github.com/yzhe819/Cosmos/raw/master/resource/img/bg2015092803.png)



当时，搭配的操作系统是[86-DOS](https://en.wikipedia.org/wiki/86-DOS)。这个操作系统需要的内存最少是32KB。我们知道，内存地址从0x0000开始编号，32KB的内存就是`0x0000～0x7FFF`。

8088芯片本身需要占用`0x0000～0x03FF`，用来保存[各种中断处理程序](https://en.wikipedia.org/wiki/BIOS_interrupt_call)的储存位置。（主引导记录本身就是中断信号INT 19h的处理程序。）所以，内存只剩下`0x0400～0x7FFF`可以使用。

**为了把尽量多的连续内存留给操作系统，主引导记录就被放到了内存地址的尾部。**由于一个扇区是512字节，主引导记录本身也会产生数据，需要另外留出512字节保存。一旦操作系统被引导并开始，引导扇区将一直不会被使用指导重启，因此操作系统和应用程序可以自由的使用32KB的最后1kb（512 + 512B）空间。所以，它的预留位置就变成了：

> ```bash
>   0x7FFF - 512 - 512 + 1 = 0x7C00 
> ```

0x7C00就是这样来的。



计算机启动后，32KB内存的使用情况如下。

> ```bash
> +--------------------- 0x0
> | Interrupts vectors
> +--------------------- 0x400
> | BIOS data area
> +--------------------- 0x5??
> | OS load area
> +--------------------- 0x7C00
> | Boot sector
> +--------------------- 0x7E00
> | Boot data/stack
> +--------------------- 0x7FFF
> | (not used)
> +--------------------- (...)
> ```



[来源](http://www.ruanyifeng.com/blog/2015/09/0x7c00.html)



### HelloOS的目录结构：

```
HelloOs
├── Makefile
├── entry.asm
├── hello.lds
├── install.md
├── main.c
├── vgastr.c
└── vgastr.h
```

### 不同文件的功能：

- entry.asm —— CRUB引导汇编代码

- vgastr.c / vgastr.h —— 通过字符模式编程操纵显卡进而操控屏幕显示

- main.c —— HelloOS的主程序（调用vgastr把Hello World打印到屏幕上）

- hello.lds —— 链接脚本(linker script)

- install.md —— 简要的安装指引

- Makefile —— 构建软件的规则，Make工具能根据这些规则自动化构建软件

  > makefile 文件中规则是这样的：首先有一个或者多个构建目标称为“target”；目标后面紧跟着用于构建该目标所需要的文件，目标下面是构建该目标所需要的命令及参数。与此同时，它也检查文件的依赖关系，如果需要的话，它会调用一些外部软件来完成任务。



### 运行HelloOS

#### 00. 系统环境

使用了最新的virtualBox 6.1.22 r144080 (Qt5.6.2)

然后安装Ubuntu 18.04.1

![image-20210703084744750](https://github.com/yzhe819/Cosmos/raw/master/resource/img/image-20210703084744750.png)





#### 01. 编译

```bash
# 在HelloOs文件夹下, 先安装一些依赖
sudo apt-get install nasm
sudo apt-get install gcc
make
```

![image-20210702214317533](https://github.com/yzhe819/Cosmos/raw/master/resource/img/image-20210702214317533.png)

当前目录树会更新成：

```
.
├── HelloOS.bin
├── HelloOS.elf
├── HelloOS.map
├── Makefile
├── entry.asm
├── entry.o
├── hello.lds
├── install.md
├── main.c
├── main.o
├── vgastr.c
├── vgastr.h
└── vgastr.o
```

编译完成后会生成**HelloOs.bin**

#### 02. 开启GRUB

我们安装了Ubuntu Linux 操作系统，GRUB 就已经存在了，只需要微调有效就能看到GRUB引导程序，运行：

```shell
sudo gedit /etc/default/grub
```

![image-20210702223016389](https://github.com/yzhe819/Cosmos/raw/master/resource/img/image-20210702223016389.png)

```
# GRUB_TIMEOUT_STYLE=hidden
# TIMEOUT改成30，在GRUB界面会等待用户30s
GRUB_TIMEOUT=30
GRUB_CMDLINE_LINUX_DEFAUL='text'
```



修改完成后，更新一下grub配置，运行：

```
sudo update-grub
```

![image-20210702223047011](https://github.com/yzhe819/Cosmos/raw/master/resource/img/image-20210702223047011.png)



#### 03. 安装

> GRUB 首先会显示启动项到屏幕，然后让我们选择启动项

首先我们需要让GRUB知道Hello OS, 篡改grud

> GRUB 根据启动项对应的信息，加载 OS 文件到内存

所以我们还需要知道boot目录挂载的分区，来让GRUB知道os文件的地址



运行：

```shell
df /boot/
```

![image-20210702224419569](https://github.com/yzhe819/Cosmos/raw/master/resource/img/image-20210702224419569.png)

其中的“sda1”就是硬盘的第一个分区，但是 GRUB 的 menuentry 中不能写 sda1，而是要写“hd0,msdos1”，这是 GRUB 的命名方式，hd0 表示第一块硬盘，结合起来就是第一块硬盘的第一个分区, 因此我们可以修改课程提供的代码为：



```assembly
menuentry 'HelloOS' {
     insmod part_msdos #GRUB加载分区模块识别分区
     insmod ext2 #GRUB加载ext文件系统模块识别ext文件系统
     set root='hd0,msdos1' #注意boot目录挂载的分区，第一块硬盘的第一个分区“sda1”
     multiboot2 /boot/HelloOS.bin #GRUB以multiboot2协议加载HelloOS.bin
     boot #GRUB启动HelloOS.bin
}
```



运行：

```shell
sudo gedit /boot/grub/grub.cfg
```

找个地方输入修改后的代码，我把他放到了末尾

![image-20210702224737581](https://github.com/yzhe819/Cosmos/raw/master/resource/img/image-20210702224737581.png)



最后一步需要我们把HelloOs.bin移到boot文件夹下，运行：

```shell
sudo nautilus #管理员权限的GUI文件夹
```

然后直接复制粘贴

![image-20210702225533997](https://github.com/yzhe819/Cosmos/raw/master/resource/img/image-20210702225533997.png)



#### 0.4 运行

重启虚拟机，可以看到Hello OS了🎉，选中后敲enter

![image-20210702225701937](https://github.com/yzhe819/Cosmos/raw/master/resource/img/image-20210702225701937.png)

![image-20210702225731399](https://github.com/yzhe819/Cosmos/raw/master/resource/img/image-20210702225731399.png)